# Providers Summary

AI providers are represented by contracts and fixtures. Provider-specific code should be tested through mock providers, retry tests, invalid JSON recovery tests, and persistence checks.

