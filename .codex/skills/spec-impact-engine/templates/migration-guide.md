# Migration Guide: {{CHANGE_TITLE}}

**Version**: {{OLD_VERSION}} → {{NEW_VERSION}}  
**Change Type**: {{CHANGE_TYPE}} <!-- BREAKING | DEPRECATION | FEATURE -->  
**Severity**: {{SEVERITY}}  
**Published**: {{DATE}}  
**Deadline**: {{MIGRATION_DEADLINE}} <!-- "Immediate" | "Before vX.Y" | "No deadline" -->  

---

## Overview

{{CHANGE_OVERVIEW_PARAGRAPH}}

This guide explains what changed, why it changed, and the exact steps to migrate your code.

**Estimated migration time**: {{ESTIMATED_MIGRATION_TIME}}

---

## What Changed

### Before ({{OLD_VERSION}})

```{{CODE_LANG}}
{{OLD_EXAMPLE_CODE}}
```

### After ({{NEW_VERSION}})

```{{CODE_LANG}}
{{NEW_EXAMPLE_CODE}}
```

### Key Differences

| Aspect | Before | After |
|--------|--------|-------|
| {{ASPECT_1}} | {{BEFORE_1}} | {{AFTER_1}} |
| {{ASPECT_2}} | {{BEFORE_2}} | {{AFTER_2}} |
| {{ASPECT_3}} | {{BEFORE_3}} | {{AFTER_3}} |

---

## Why It Changed

{{REASON_FOR_CHANGE}}

**Business context**: {{BUSINESS_REASON}}  
**Technical context**: {{TECHNICAL_REASON}}  
**Alternative considered**: {{ALTERNATIVE_CONSIDERED}} — rejected because {{REJECTION_REASON}}

---

## Migration Steps

### Step 1: {{STEP_1_TITLE}}

{{STEP_1_DESCRIPTION}}

```{{CODE_LANG}}
// Before
{{STEP_1_BEFORE_CODE}}

// After
{{STEP_1_AFTER_CODE}}
```

**Verification**: {{STEP_1_VERIFICATION}}

---

### Step 2: {{STEP_2_TITLE}}

{{STEP_2_DESCRIPTION}}

```{{CODE_LANG}}
// Before
{{STEP_2_BEFORE_CODE}}

// After
{{STEP_2_AFTER_CODE}}
```

**Verification**: {{STEP_2_VERIFICATION}}

---

### Step 3: {{STEP_3_TITLE}} (if applicable)

{{STEP_3_DESCRIPTION}}

```{{CODE_LANG}}
// Before
{{STEP_3_BEFORE_CODE}}

// After
{{STEP_3_AFTER_CODE}}
```

**Verification**: {{STEP_3_VERIFICATION}}

---

## Code Search Patterns

Use these patterns to find all affected code in your codebase:

```bash
# Find usages of the old field/method/endpoint
grep -rn "{{OLD_PATTERN}}" src/
grep -rn "{{OLD_PATTERN}}" tests/
grep -rn "{{OLD_PATTERN}}" playwright/

# After migration, verify no old pattern remains
grep -rn "{{OLD_PATTERN}}" . --include="*.ts" --include="*.js" --include="*.json"
# Should return 0 results
```

---

## Affected File Types

| File Pattern | Change Required | Example |
|-------------|----------------|---------|
| `src/api/**/*.ts` | {{API_CHANGE}} | `{{API_EXAMPLE}}` |
| `tests/**/*.test.ts` | {{TEST_CHANGE}} | `{{TEST_EXAMPLE}}` |
| `types/**/*.ts` | {{TYPE_CHANGE}} | `{{TYPE_EXAMPLE}}` |
| `contracts/**/*.json` | {{CONTRACT_CHANGE}} | `{{CONTRACT_EXAMPLE}}` |

---

## Common Migration Pitfalls

### ❌ Pitfall 1: {{PITFALL_1_TITLE}}

**Symptom**: {{PITFALL_1_SYMPTOM}}

**Incorrect approach**:
```{{CODE_LANG}}
{{PITFALL_1_WRONG_CODE}}
```

**Correct approach**:
```{{CODE_LANG}}
{{PITFALL_1_CORRECT_CODE}}
```

---

### ❌ Pitfall 2: {{PITFALL_2_TITLE}}

**Symptom**: {{PITFALL_2_SYMPTOM}}

**Incorrect approach**:
```{{CODE_LANG}}
{{PITFALL_2_WRONG_CODE}}
```

**Correct approach**:
```{{CODE_LANG}}
{{PITFALL_2_CORRECT_CODE}}
```

---

## Validation Checklist

After completing migration, verify:

- [ ] All uses of `{{OLD_PATTERN}}` replaced with `{{NEW_PATTERN}}`
- [ ] TypeScript compiles without errors: `tsc --noEmit`
- [ ] Unit tests pass: `npm test`
- [ ] Integration tests pass: `npm run test:integration`
- [ ] E2E tests pass: `npx playwright test`
- [ ] No `{{OLD_PATTERN}}` in grep results
- [ ] API responses match new contract schema
- [ ] Breaking change entry in SPEC_CHANGELOG.md

---

## Rollback Plan

If migration causes unexpected issues:

```bash
# Option 1: Revert specific files
git revert HEAD -- path/to/affected/file.ts

# Option 2: Revert entire migration commit
git revert <migration-commit-hash>

# After rollback:
npm test  # Verify passing again
# Then investigate the issue before re-attempting migration
```

**Rollback window**: {{ROLLBACK_DEADLINE}}  
**Note**: {{ROLLBACK_NOTE}}

---

## Support

- **Questions**: Open an issue or ask in `#dev-help` Slack channel
- **Bug reports**: Tag `migration-{{OLD_VERSION}}-to-{{NEW_VERSION}}`
- **Emergency contact**: {{EMERGENCY_CONTACT}}

---

## Timeline

| Date | Milestone |
|------|-----------|
| {{DATE}} | Migration guide published |
| {{DEADLINE_SOFT}} | Recommended migration complete |
| {{DEADLINE_HARD}} | Old API/field/behavior removed |
| {{REMOVAL_DATE}} | {{OLD_VERSION}} support dropped |

---

*Generated by spec-impact-engine v1.0 | Genesis Codex Harness*  
*Last updated: {{DATE}}*
